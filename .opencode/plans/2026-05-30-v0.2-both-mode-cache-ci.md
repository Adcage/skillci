# SkillCI v0.2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现 Both Mode、LLM Judge Cache 和 CI Readiness，让 SkillCI 可以同时运行本地和 LLM 判断，并具备进入 CI 工作流的基础能力。

**Architecture:** 基于现有 runner 架构，新增 `run_both_test` 函数协调 local 和 llm 评估器；新增 `skillci/cache/` 模块实现基于内容哈希的缓存系统；扩展报告系统支持 disagreement 展示。

**Tech Stack:** Python 3.10+, Typer, Pydantic, Rich, hashlib (stdlib)

---

## Task 1: 实现 Both Mode 核心逻辑

**Files:**
- Modify: `skillci/runner.py`
- Modify: `skillci/cli.py`
- Test: `tests/test_runner.py`

**Step 1: 在 runner.py 添加 run_both_test 函数**

```python
def run_both_test(
    skill_path: Path,
    config_path: Path | None = None,
    provider_name: str = "openai",
) -> SkillCIReport:
    skill = parse_skill(skill_path)
    config = parse_config(config_path or skill.path / "skillci.yaml")
    static_health = run_static_health_checks(skill)

    local_results = [
        evaluate_local_trigger(skill, case, config.thresholds)
        for case in config.cases
    ]

    llm_results = [
        run_llm_judge(
            skill,
            case,
            config.thresholds,
            provider_name=provider_name,
            judge_config=config.judge,
        )
        for case in config.cases
    ]

    local_metrics = calculate_metrics(local_results)
    llm_metrics = calculate_metrics(llm_results)

    valid_confidences = [
        result.confidence for result in llm_results if result.confidence is not None
    ]
    llm_average_confidence = (
        round(sum(valid_confidences) / len(valid_confidences), 4)
        if valid_confidences
        else None
    )

    judge_disagreements = []
    for local, llm in zip(local_results, llm_results):
        if llm.error:
            continue
        if local.actual_trigger != llm.actual_trigger:
            judge_disagreements.append(
                JudgeDisagreement(
                    case_name=local.case_name,
                    expected_trigger=local.expected_trigger,
                    local_actual=local.actual_trigger,
                    llm_actual=llm.actual_trigger,
                    local_score=local.trigger_score,
                    llm_confidence=llm.confidence,
                )
            )

    passed = static_health.passed and all(r.passed for r in local_results)

    return SkillCIReport(
        skill_name=skill.name,
        skill_path=str(skill.path),
        static_health=static_health,
        local_results=local_results,
        llm_results=llm_results,
        local_metrics=local_metrics,
        llm_metrics=llm_metrics,
        llm_average_confidence=llm_average_confidence,
        judge_disagreement_count=len(judge_disagreements),
        judge_disagreements=judge_disagreements,
        passed=passed,
    )
```

**Step 2: 修改 cli.py 支持 --mode both**

在 `test` 命令中修改 mode 参数处理：

```python
@app.command()
def test(
    skill_path: Path,
    mode: str = typer.Option("local", help="Trigger test mode: local, llm, or both."),
    json_output: bool = typer.Option(False, "--json", help="Output JSON report."),
    provider: str = typer.Option("openai", help="LLM judge provider. Use mock for tests."),
    compare: str | None = typer.Option(None, help="Compare with baseline."),
) -> None:
    """Run Skill trigger tests."""
    if mode == "local":
        report = run_local_test(skill_path)
    elif mode == "llm":
        report = run_llm_test(skill_path, provider_name=provider)
    elif mode == "both":
        report = run_both_test(skill_path, provider_name=provider)
    else:
        typer.echo(f"Unsupported mode: {mode}. Use local, llm, or both.", err=True)
        raise typer.Exit(2)
    # ... 其余逻辑保持不变
```

**Step 3: 运行测试验证**

Run: `pytest tests/test_runner.py -v`
Expected: 现有测试通过

**Step 4: 提交**

```bash
git add skillci/runner.py skillci/cli.py
git commit -m "feat: add both mode support in runner and cli"
```

---

## Task 2: 实现 JudgeDisagreement 模型和报告字段

**Files:**
- Modify: `skillci/schema/report.py`
- Modify: `skillci/schema/result.py`
- Test: `tests/test_models.py`

**Step 1: 在 result.py 添加 JudgeDisagreement 模型**

```python
class JudgeDisagreement(BaseModel):
    case_name: str
    expected_trigger: bool
    local_actual: bool
    llm_actual: bool | None
    local_score: float | None = None
    llm_confidence: float | None = None
    reason: str | None = None
```

**Step 2: 在 report.py 添加 disagreement 字段**

```python
from skillci.schema.result import JudgeDisagreement

class SkillCIReport(BaseModel):
    skill_name: str
    skill_path: str
    static_health: StaticHealthResult
    local_results: list[LocalTriggerResult] = Field(default_factory=list)
    llm_results: list[LLMTriggerResult] = Field(default_factory=list)
    local_metrics: TriggerMetrics | None = None
    llm_metrics: TriggerMetrics | None = None
    llm_average_confidence: float | None = None
    judge_disagreement_count: int = 0
    judge_disagreements: list[JudgeDisagreement] = Field(default_factory=list)
    passed: bool
```

**Step 3: 运行测试验证**

Run: `pytest tests/test_models.py -v`
Expected: 测试通过

**Step 4: 提交**

```bash
git add skillci/schema/result.py skillci/schema/report.py
git commit -m "feat: add JudgeDisagreement model and report fields"
```

---

## Task 3: 更新报告系统展示 Disagreement

**Files:**
- Modify: `skillci/report/terminal_report.py`
- Modify: `skillci/report/markdown_report.py`
- Modify: `skillci/report/json_report.py`
- Test: `tests/test_report.py`

**Step 1: 更新 terminal_report.py**

在 `render_report` 函数中添加 disagreement 展示：

```python
if report.judge_disagreements:
    console.print("\nJudge Disagreements", style="bold yellow")
    console.print(f"  Count: {report.judge_disagreement_count}")
    for d in report.judge_disagreements:
        console.print(
            f"  DISAGREE {d.case_name}: local={d.local_actual}, "
            f"llm={d.llm_actual}, expected={d.expected_trigger}"
        )
```

**Step 2: 更新 markdown_report.py**

在 `render_markdown` 函数中添加 disagreement 部分：

```python
if report.judge_disagreements:
    lines.append("## Judge Disagreements")
    lines.append("")
    lines.append(f"Count: {report.judge_disagreement_count}")
    lines.append("")
    lines.append("| Case | Expected | Local | LLM |")
    lines.append("| --- | --- | --- | --- |")
    for d in report.judge_disagreements:
        lines.append(
            f"| {d.case_name} "
            f"| {d.expected_trigger} "
            f"| {d.local_actual} "
            f"| {d.llm_actual} |"
        )
    lines.append("")
```

**Step 3: 运行测试验证**

Run: `pytest tests/test_report.py -v`
Expected: 测试通过

**Step 4: 提交**

```bash
git add skillci/report/terminal_report.py skillci/report/markdown_report.py
git commit -m "feat: display judge disagreements in terminal and markdown reports"
```

---

## Task 4: 实现 LLM Judge Cache 模块

**Files:**
- Create: `skillci/cache/__init__.py`
- Create: `skillci/cache/cache_key.py`
- Create: `skillci/cache/judge_cache.py`
- Test: `tests/test_judge_cache.py`

**Step 1: 创建 cache_key.py**

```python
import hashlib
import json

from skillci.evaluator.prompt import PROMPT_VERSION
from skillci.schema.config import JudgeConfig, SkillTestCase
from skillci.schema.skill import Skill


def build_judge_cache_key(
    skill: Skill,
    case: SkillTestCase,
    config: JudgeConfig,
) -> str:
    key_data = {
        "skill_name": skill.name,
        "skill_description": skill.description,
        "skill_body_excerpt": skill.body[:500] if skill.body else "",
        "case_name": case.name,
        "case_input": case.input,
        "provider": config.provider,
        "base_url": config.base_url,
        "model": config.model,
        "prompt_version": PROMPT_VERSION,
    }
    key_string = json.dumps(key_data, sort_keys=True, ensure_ascii=False)
    return hashlib.sha256(key_string.encode("utf-8")).hexdigest()
```

**Step 2: 创建 judge_cache.py**

```python
import json
from datetime import datetime, timezone
from pathlib import Path

from skillci.schema.result import LLMTriggerResult


class JudgeCache:
    def __init__(self, cache_dir: Path | None = None):
        self.cache_dir = cache_dir or Path(".skillci/cache")
        self.cache_dir.mkdir(parents=True, exist_ok=True)

    def _cache_path(self, key: str) -> Path:
        return self.cache_dir / f"{key}.json"

    def get(self, key: str) -> LLMTriggerResult | None:
        path = self._cache_path(key)
        if not path.exists():
            return None
        try:
            data = json.loads(path.read_text(encoding="utf-8"))
            return LLMTriggerResult(**data)
        except (json.JSONDecodeError, Exception):
            return None

    def set(self, key: str, result: LLMTriggerResult) -> None:
        path = self._cache_path(key)
        data = result.model_dump()
        data["cached_at"] = datetime.now(timezone.utc).isoformat()
        path.write_text(
            json.dumps(data, indent=2, ensure_ascii=False),
            encoding="utf-8",
        )
```

**Step 3: 创建测试文件**

```python
# tests/test_judge_cache.py
from pathlib import Path
from skillci.cache.cache_key import build_judge_cache_key
from skillci.cache.judge_cache import JudgeCache
from skillci.schema.config import JudgeConfig, SkillTestCase
from skillci.schema.result import LLMTriggerResult
from skillci.schema.skill import Skill


def test_cache_key_deterministic():
    skill = Skill(name="test", description="desc", body="body", path=Path("."))
    case = SkillTestCase(name="case1", input="input", expected_trigger=True)
    config = JudgeConfig()
    key1 = build_judge_cache_key(skill, case, config)
    key2 = build_judge_cache_key(skill, case, config)
    assert key1 == key2


def test_cache_set_and_get(tmp_path):
    cache = JudgeCache(cache_dir=tmp_path)
    result = LLMTriggerResult(
        case_name="test",
        expected_trigger=True,
        actual_trigger=True,
        passed=True,
        confidence=0.9,
    )
    cache.set("testkey", result)
    cached = cache.get("testkey")
    assert cached is not None
    assert cached.case_name == "test"
    assert cached.actual_trigger is True


def test_cache_miss_returns_none(tmp_path):
    cache = JudgeCache(cache_dir=tmp_path)
    assert cache.get("nonexistent") is None
```

**Step 4: 运行测试验证**

Run: `pytest tests/test_judge_cache.py -v`
Expected: 所有测试通过

**Step 5: 提交**

```bash
git add skillci/cache/ tests/test_judge_cache.py
git commit -m "feat: implement LLM judge cache module"
```

---

## Task 5: 集成 Cache 到 LLM Judge

**Files:**
- Modify: `skillci/evaluator/llm_trigger_judge.py`
- Modify: `skillci/schema/config.py`
- Test: `tests/test_llm_trigger_judge.py`

**Step 1: 修改 llm_trigger_judge.py 集成缓存**

```python
from skillci.cache.cache_key import build_judge_cache_key
from skillci.cache.judge_cache import JudgeCache
from skillci.providers.base import JudgeProvider
from skillci.providers.mock_provider import MockJudgeProvider
from skillci.providers.openai_provider import OpenAIJudgeProvider
from skillci.schema.config import JudgeConfig, SkillTestCase, ThresholdConfig
from skillci.schema.result import LLMTriggerResult
from skillci.schema.skill import Skill

_PROVIDERS: dict[str, type[JudgeProvider]] = {
    "mock": MockJudgeProvider,
    "openai": OpenAIJudgeProvider,
}

_cache: JudgeCache | None = None


def _get_cache() -> JudgeCache:
    global _cache
    if _cache is None:
        _cache = JudgeCache()
    return _cache


def _provider_for_name(name: str) -> JudgeProvider:
    provider_cls = _PROVIDERS.get(name)
    if provider_cls is None:
        raise ValueError(f"Unsupported judge provider: {name}")
    return provider_cls()


def run_llm_judge(
    skill: Skill,
    case: SkillTestCase,
    thresholds: ThresholdConfig,
    provider_name: str = "openai",
    judge_config: JudgeConfig | None = None,
) -> LLMTriggerResult:
    config = judge_config or JudgeConfig()

    if config.cache:
        cache = _get_cache()
        cache_key = build_judge_cache_key(skill, case, config)
        cached = cache.get(cache_key)
        if cached is not None:
            return cached

    provider = _provider_for_name(provider_name)
    result = provider.judge_trigger(skill, case, config)

    if result.error:
        return result

    if result.actual_trigger is None:
        final_result = LLMTriggerResult(
            case_name=case.name,
            expected_trigger=case.expected_trigger,
            actual_trigger=None,
            confidence=result.confidence,
            passed=False,
            reason=result.reason,
            raw_response=result.raw_response,
            error="LLM judge did not return should_trigger",
        )
    else:
        passed = result.actual_trigger == case.expected_trigger
        final_result = LLMTriggerResult(
            case_name=case.name,
            expected_trigger=case.expected_trigger,
            actual_trigger=result.actual_trigger,
            confidence=result.confidence,
            passed=passed,
            reason=result.reason,
            raw_response=result.raw_response,
        )

    if config.cache:
        cache.set(cache_key, final_result)

    return final_result
```

**Step 2: 运行测试验证**

Run: `pytest tests/test_llm_trigger_judge.py -v`
Expected: 测试通过

**Step 3: 提交**

```bash
git add skillci/evaluator/llm_trigger_judge.py
git commit -m "feat: integrate cache into LLM judge"
```

---

## Task 6: 添加 --no-cache CLI 参数

**Files:**
- Modify: `skillci/cli.py`
- Test: `tests/test_cli.py`

**Step 1: 修改 cli.py 添加 --no-cache 参数**

```python
@app.command()
def test(
    skill_path: Path,
    mode: str = typer.Option("local", help="Trigger test mode: local, llm, or both."),
    json_output: bool = typer.Option(False, "--json", help="Output JSON report."),
    provider: str = typer.Option("openai", help="LLM judge provider. Use mock for tests."),
    compare: str | None = typer.Option(None, help="Compare with baseline."),
    no_cache: bool = typer.Option(False, "--no-cache", help="Disable LLM judge cache."),
) -> None:
    """Run Skill trigger tests."""
    if mode == "local":
        report = run_local_test(skill_path)
    elif mode == "llm":
        report = run_llm_test(skill_path, provider_name=provider, use_cache=not no_cache)
    elif mode == "both":
        report = run_both_test(skill_path, provider_name=provider, use_cache=not no_cache)
    else:
        typer.echo(f"Unsupported mode: {mode}. Use local, llm, or both.", err=True)
        raise typer.Exit(2)
    # ... 其余逻辑保持不变
```

**Step 2: 修改 runner.py 支持 use_cache 参数**

在 `run_llm_test` 和 `run_both_test` 中添加 `use_cache` 参数，并传递给 `run_llm_judge`。

**Step 3: 运行测试验证**

Run: `pytest tests/test_cli.py -v`
Expected: 测试通过

**Step 4: 提交**

```bash
git add skillci/cli.py skillci/runner.py
git commit -m "feat: add --no-cache CLI flag"
```

---

## Task 7: 添加 GitHub Actions 使用文档

**Files:**
- Create: `docs/github-actions.md`
- Modify: `README.md`
- Modify: `README_zh.md`

**Step 1: 创建 docs/github-actions.md**

内容包括：
- Local mode workflow 示例
- LLM mode workflow 示例
- Both mode workflow 示例
- OPENAI_API_KEY secret 配置说明
- paths 触发条件说明
- 缓存配置说明

**Step 2: 更新 README.md 添加链接**

在适当位置添加：

```markdown
## CI Integration

See [GitHub Actions Guide](docs/github-actions.md) for CI/CD setup instructions.
```

**Step 3: 提交**

```bash
git add docs/github-actions.md README.md README_zh.md
git commit -m "docs: add GitHub Actions usage guide"
```

---

## Task 8: 添加 PR 友好的 GitHub 报告格式

**Files:**
- Modify: `skillci/report/markdown_report.py`
- Modify: `skillci/cli.py`
- Test: `tests/test_report.py`

**Step 1: 添加 render_github_markdown 函数**

```python
def render_github_markdown(report: SkillCIReport) -> str:
    lines: list[str] = []
    lines.append("## SkillCI Check")
    lines.append("")
    lines.append("| Section | Result |")
    lines.append("|---|---|")

    static_status = "passed" if report.static_health.passed else "failed"
    lines.append(f"| Static Health | {static_status} |")

    if report.local_metrics:
        lines.append(f"| Local F1 | {report.local_metrics.f1:.2f} |")

    if report.llm_metrics:
        lines.append(f"| LLM F1 | {report.llm_metrics.f1:.2f} |")

    lines.append(f"| Judge Disagreements | {report.judge_disagreement_count} |")

    regression_count = 0  # 需要从 compare 结果获取
    lines.append(f"| Regressions | {regression_count} |")

    final_status = "passed" if report.passed else "failed"
    lines.append(f"| Final Result | **{final_status}** |")
    lines.append("")

    return "\n".join(lines)
```

**Step 2: 修改 cli.py 支持 --format github**

```python
@app.command()
def report(
    skill_path: Path,
    format: str = typer.Option("markdown", help="Report format: markdown or github."),
    output: Path | None = typer.Option(None, help="Optional output file path."),
) -> None:
    """Generate a SkillCI report."""
    current_report = run_local_test(skill_path)

    if format == "markdown":
        content = render_markdown(current_report)
    elif format == "github":
        content = render_github_markdown(current_report)
    else:
        typer.echo(f"Unsupported format: {format}. Use markdown or github.", err=True)
        raise typer.Exit(2)

    if output:
        output.parent.mkdir(parents=True, exist_ok=True)
        output.write_text(content, encoding="utf-8")
        typer.echo(f"Wrote report to {output}")
    else:
        typer.echo(content)
```

**Step 3: 运行测试验证**

Run: `pytest tests/test_report.py -v`
Expected: 测试通过

**Step 4: 提交**

```bash
git add skillci/report/markdown_report.py skillci/cli.py
git commit -m "feat: add PR-friendly github report format"
```

---

## Task 9: 完善测试覆盖

**Files:**
- Create: `tests/test_both_mode.py`
- Modify: `tests/test_runner.py`

**Step 1: 创建 both mode 测试**

```python
# tests/test_both_mode.py
from pathlib import Path
from skillci.runner import run_both_test
from skillci.schema.skill import Skill


def test_both_mode_returns_disagreements(tmp_path):
    # 准备测试数据，构造 local 和 llm 结果不一致的场景
    pass


def test_both_mode_empty_disagreements(tmp_path):
    # 测试没有 disagreement 的情况
    pass
```

**Step 2: 运行完整测试套件**

Run: `pytest -v`
Expected: 所有测试通过

**Step 3: 提交**

```bash
git add tests/
git commit -m "test: add both mode and cache tests"
```

---

## Task 10: 版本号更新和最终验证

**Files:**
- Modify: `pyproject.toml`
- Modify: `skillci/__init__.py`
- Modify: `README.md`
- Modify: `README_zh.md`

**Step 1: 更新版本号到 0.2.0**

```toml
# pyproject.toml
version = "0.2.0"
```

```python
# skillci/__init__.py
__version__ = "0.2.0"
```

**Step 2: 更新 README badges**

更新 release badge 版本号。

**Step 3: 运行完整验证**

```bash
ruff check .
pytest -v
```

**Step 4: 提交**

```bash
git add pyproject.toml skillci/__init__.py README.md README_zh.md
git commit -m "chore: bump version to 0.2.0"
```

---

## 验收检查清单

完成所有任务后，运行以下验收命令：

```bash
# 1. Both mode 验收
skillci test examples/api-doc-writer --mode both --provider mock

# 2. Cache 验收（运行两次，第二次应命中缓存）
skillci test examples/api-doc-writer --mode llm --provider mock
skillci test examples/api-doc-writer --mode llm --provider mock

# 3. No-cache 验收
skillci test examples/api-doc-writer --mode llm --provider mock --no-cache

# 4. GitHub 报告格式验收
skillci report examples/api-doc-writer --format github

# 5. 工程验收
ruff check .
pytest -v
```

**预期结果：**
- Both mode 同时输出 Local Trigger Check 和 LLM Trigger Check
- 显示 Judge Disagreements: 0（对于 api-doc-writer 示例）
- Cache 第二次运行时命中
- GitHub 报告格式正确显示
- 所有测试通过，lint 无错误
